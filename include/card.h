#include "../include/constants.h"
#include <stdio.h>
typedef struct Card {
  char state[50];
  char city[50];
  char code[5];
  unsigned long int population;
  float area;
  float pib;
  int turist_point;
  float density;
  float gpd;
  float super_power;
} Card;
